{% import 'templates/alert/code.jinja2' as code with context %}

{# This really has no use if we rely on user_label #}
//   <s.100> Name
//   <i> A name which define the alert in the code
//   <id> prox_alert_name
#define {{ code.get_name_define() }}  "alert"

//   <o> ID <1-65535>
//   <i> An id is crucial and must be consistent with an id on the Proximetry Portal
//   <id> prox_alert_id
#define {{ code.get_id_define() }}  1
